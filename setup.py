
import sys
import os

import setuptools
from setuptools import setup, find_packages

setup(
      name="coherence",
      version="0.1",
      description="""Coherence quoi""",
      author="not me",
      packages=find_packages()
      )
